<%= turbo_stream.replace("cart-dropdown-panel", partial: "shared/dropdown", locals: { cart: @cart }) %>

<%= turbo_stream.replace("cart-count", partial: "shared/cart_count", locals: { cart: @cart }) %>
